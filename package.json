{
    "name": "salesbot-root",
    "version": "1.0.0",
    "description": "Salesbot Monorepo Root",
    "scripts": {
        "postinstall": "cd client && npm install && cd ../server && npm install",
        "build": "cd client && npm run build",
        "start": "cd server && node server.js"
    },
    "engines": {
        "node": ">=18.0.0"
    },
    "dependencies": {
        "concurrently": "^8.2.2"
    }
}